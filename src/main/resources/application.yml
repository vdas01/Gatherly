spring:
  application:
    name: gatherly

  datasource:
    url: jdbc:mysql://localhost:3306/gatherly
    username: root
    password: vishaliphone

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
